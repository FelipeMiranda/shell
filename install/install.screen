screen -t normal -p 0
source $HOME/.screenrc
split -v          # Make second split
focus             # Switch to second split
screen -t install -p 1 $CDSHELL/install.sh # Open first screen and launch first script
focus
resize +10
